# Project Rules

- **Git Branch Restriction**: NEVER push, commit, or merge code directly to the `main` branch. All work and Git pushes must be done on the `PEE` branch. Do not target `main` for any actions unless explicitly instructed by the user.
